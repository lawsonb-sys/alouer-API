{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": {
        "tsConfig": "tsconfig.build.json"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/$1" },
    { "src": "/api/(.*)", "dest": "/api" }
  ]
}
